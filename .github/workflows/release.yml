name: Build installers

# Triggers on version tags (v1.2.3). Artifacts go to Actions, not GitHub Release.
# Manual run: Actions → Build installers → Run workflow (branch main).
# Upload installers to Release manually; see docs/softphone/GitHub-Releases-Update-Guide.md

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:
  release:
    env:
      EP_PUBLISH: never
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            build_script: build:win
            artifact_glob: dist/win/**
          - os: macos-latest
            build_script: build:mac
            artifact_glob: dist/mac/**
          - os: ubuntu-latest
            build_script: build:linux
            artifact_glob: dist/linux/**

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test

      - name: Build installer
        run: npm run ${{ matrix.build_script }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: installer-${{ matrix.os }}
          path: ${{ matrix.artifact_glob }}
          if-no-files-found: error
