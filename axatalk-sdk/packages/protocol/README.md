# @axatalk/protocol

Protocol schemas and public TypeScript contracts for the Axatalk local protocol.

**Status:** placeholder package (SDK-00). No public API yet. Implementation starts in SDK-02.

This package must remain independent from `@axatalk/sdk` and from Axatalk Desktop source.
