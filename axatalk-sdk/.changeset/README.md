# Changesets

Release scaffolding for `@axatalk/protocol` and `@axatalk/sdk`.

SDK-00 does **not** publish packages. Changesets and `publishConfig.provenance`
are prepared for a later release unit (SDK-10) after security and desktop gates.
