# Baked into production installers — real SIP/WebRTC adapters (not mock).
VITE_ADAPTER_MODE=real
# Axatalk update manifest (must exist on main) — guides/GitHub-Releases-Update-Guide.md
VITE_UPDATE_MANIFEST_URL=https://raw.githubusercontent.com/HailRase/axatalk-releases/main/update-manifest.json
