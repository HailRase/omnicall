Проверь **Feature Registry** evidence paths vs файловая система.

```bash
npm run registry:check
```

Прочитай `docs/softphone/Feature-Registry.md` — spot-check статусы F-XXX vs `STATUS.md`.

Формат: `.cursor/skills/_shared/response-contract.md`.
Статус `gate_pass` если скрипт green и нет Blocker расхождений; иначе `gate_fail`.

Не меняй код — только отчёт с Severity.
