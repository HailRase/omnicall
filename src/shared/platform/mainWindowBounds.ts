/**
 * - Purpose: canonical initial BrowserWindow bounds for softphone shell startup.
 * - Inputs: none.
 * - Outputs: width, height, and minimum size constants.
 */
export const MAIN_WINDOW_INITIAL_BOUNDS = {
  width: 360,
  height: 625,
  minWidth: 360,
  minHeight: 560,
} as const;
