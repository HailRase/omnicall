/**
 * Startup splash / BrowserWindow background before renderer theme applies.
 * Keep in sync with `--color-bg-app` in `src/renderer/styles/tokens.css`
 * and the inline splash styles in `src/renderer/index.html`.
 */
export const STARTUP_SPLASH_BG_LIGHT = "#f3f6fb";
export const STARTUP_SPLASH_BG_DARK = "#111722";
