declare const classes: {
  readonly compactInput: string;
  readonly compactInputInvalid: string;
  readonly root: string;
  readonly suffixAccessible: string;
  readonly touchTarget: string;
};
export default classes;
