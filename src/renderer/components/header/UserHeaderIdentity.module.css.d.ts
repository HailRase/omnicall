declare const classes: {
  readonly displayName: string;
  readonly identity: string;
  readonly sipStatus: string;
  readonly sipStatusBlock: string;
  readonly sipStatusTimer: string;
  readonly statusConnecting: string;
  readonly statusDisconnected: string;
  readonly statusDnd: string;
  readonly statusIdle: string;
  readonly statusNotRegistered: string;
  readonly statusReconnecting: string;
  readonly statusRegistered: string;
  readonly statusRegistering: string;
};
export default classes;
