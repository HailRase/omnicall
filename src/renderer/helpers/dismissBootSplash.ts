/**
 * @deprecated Prefer `dismissBootSplash` from `./bootSplashDom.js` (single-stage splash).
 * Re-export kept so existing imports keep working.
 */
export { dismissBootSplash } from "./bootSplashDom.js";
